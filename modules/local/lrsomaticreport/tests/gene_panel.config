// modules.config is not loaded here; reproduce its --gene-panel arg (staged TSVs live at gene_panels/<base name>)
process {
    withName: 'LRSOMATICREPORT' {
        ext.args = "--gene-panel 'gene_panels/test_panel.tsv'"
    }
}
