// conf/modules.config is not loaded in module-level nf-test, so reproduce its --gene-panel argument (staged TSVs live at gene_panels/<base name>)
process {
    withName: 'LRSOMATICREPORT' {
        ext.args = "--gene-panel 'gene_panels/test_panel.tsv'"
    }
}
